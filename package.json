{
  "name": "registry-demo",
  "version": "1.0.0",
  "description": "A registry for simple JSON files",
  "repository": "https://github.com/iamdanfox/registry-demo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "mkdir -p build/release && cp index.html build/release && cp -R api build/release && cp now.json build/release",
    "deploy": "cd build/release && URL=$(now --token \"$NOW_TOKEN\") && now alias \"$URL\" registry-demo --token \"$NOW_TOKEN\""
  },
  "author": "iamdanfox",
  "license": "MIT",
  "devDependencies": {
    "now": "^12.1.5",
    "typescript": "^3.1.6"
  }
}
